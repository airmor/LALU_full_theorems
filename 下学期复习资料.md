# 线性代数下学期期末复习资料

[TOC]

---

## 第一章 线性空间进阶

> **复习要点**：线性空间是全书最基本的结构。8条公理定义了"可以线性组合"的集合。核心思路链：公理 → 子空间（非空+封闭）→ 线性相关/无关（判断是否有非平凡零组合）→ 基（极大无关组/极小生成元）→ 维数（基的势，良定义）→ 坐标映射（将抽象向量转化为 $\mathbf{F}^n$ 中坐标）→ 线性映射（保线性的态射，由基的像唯一确定）。本章进阶内容：模（环上的线性空间）、域扩张下的线性空间结构、同构的本质是维数相等。

> **知识链**：8条公理 → 子空间判别（非空+加乘封闭）→ $\operatorname{span}(S)$ 最小子空间 → 线性相关/无关（齐次组合方程）→ 源泉定理（极大无关组的基数相等）→ 基与维数 → 替换定理 → 坐标映射 $V\cong\mathbf{F}^n$ → 秩 $\dim\operatorname{span}(S)$ → 线性同构 $\iff$ 维数相等 → 模（环上的推广）

### 1.1 线性空间公理与基本结构

**定义 1.1（线性空间）** 设 $V$ 为非空集合，$\mathbf{F}$ 为数域。若 $V$ 上定义了加法与数乘，满足：
- 加法：结合律、有单位元 $\vec{0}$、每元有逆元、交换律
- 数乘：$1\alpha=\alpha$、$\lambda(\mu\alpha)=(\lambda\mu)\alpha$、$(\lambda+\mu)\alpha=\lambda\alpha+\mu\alpha$、$\lambda(\alpha+\beta)=\lambda\alpha+\lambda\beta$

则称 $V$ 为 $\mathbf{F}$ 上的线性空间。

**定理 1.1（基本运算性质）** 
1. $\lambda\vec{0}=\vec{0}$，$0\alpha=\vec{0}$
2. 若 $\lambda\alpha=\vec{0}$，则 $\lambda=0$ 或 $\alpha=\vec{0}$
3. 加法交换律可由其余7条公理推出

**定义 1.2（线性子空间）** 设 $W\subseteq V$ 非空。若 $W$ 对加法与数乘封闭，则 $W$ 为 $V$ 的子空间。

**定理 1.2（子空间判别）** 非空子集 $W$ 是子空间 $\iff$ 对任意 $\alpha,\beta\in W$，$\lambda,\mu\in\mathbf{F}$，有 $\lambda\alpha+\mu\beta\in W$。

**定义 1.3（线性扩张）** $\operatorname{span}(S)=\{\sum\lambda_i\alpha_i\mid\lambda_i\in\mathbf{F},\alpha_i\in S\}$ 是包含 $S$ 的最小子空间。

### 1.2 线性相关性与基

**定义 1.4（线性相关/无关）** 若存在不全为零的 $\lambda_i$ 使 $\sum\lambda_i\alpha_i=\vec{0}$，则向量组线性相关；否则线性无关。

**定理 1.3（向量组线相关的等价表述）** 向量组线性相关 $\iff$ 其中至少有一个向量可由其余向量线性表示。

**定义 1.5（基与维数）** 既是线性无关组又是生成元集的向量组称为基；基的基数（向量个数）称为维数，记为 $\dim V$。

**定理 1.4（源泉定理）** 任意两个极大线性无关组包含相同个数的向量。因此维数是良定义的。

**定理 1.5（替换定理）** 设 $\{\alpha_1,\ldots,\alpha_n\}$ 为基，$\beta=\sum c_i\alpha_i$。若 $c_k\neq0$，则用 $\beta$ 替换 $\alpha_k$ 后仍为基。

### 1.3 线性映射与同构

**定义 1.6（线性映射与同构）** $f:V\to W$ 称为线性映射，若 $f(\alpha+\beta)=f(\alpha)+f(\beta)$，$f(\lambda\alpha)=\lambda f(\alpha)$。双射的线性映射称为同构，记 $V\cong W$。

**定理 1.6（构造定理）** 线性映射由基的像唯一确定：若 $\{e_i\}$ 为 $V$ 的基，$\{w_i\}\subseteq W$ 任意，则存在唯一线性映射 $f$ 使 $f(e_i)=w_i$。

**定理 1.7（像与核）** $\operatorname{im}f$ 是 $W$ 的子空间；$\ker f$ 是 $V$ 的子空间。$f$ 单 $\iff\ker f=\{0\}$。

**定理 1.8（秩-零化度公式 / 线性映射基本定理）** 若 $V$ 有限维，则
$$\dim V=\dim\ker f+\dim\operatorname{im}f.$$

**定理 1.9（同构的维数判据）** 有限维空间 $V\cong W\iff\dim V=\dim W$。当 $\dim V=\dim W$ 时，线性映射 $f$ 单 $\iff$ 满 $\iff$ 可逆。

### 1.4 进阶：域扩张与模

**定理 1.10（域扩张下的线性空间）** 若 $\mathbf{E}\subseteq\mathbf{F}$ 是域扩张，则 $\mathbf{F}$ 是 $\mathbf{E}$-线性空间；若 $V$ 是 $\mathbf{F}$-空间，则 $V$ 也是 $\mathbf{E}$-空间。

**定义 1.7（模）** 设 $R$ 为环，$M$ 为 Abel 群。若定义了数乘 $R\times M\to M$ 满足与线性空间类似的四条公理（$1\alpha=\alpha$，$r(s\alpha)=(rs)\alpha$，分配律），则 $M$ 为 $R$-模。模是线性空间的推广（系数取自环而非域）。

---

## 第二章 线性空间的积与商

> **复习要点**：这一章研究如何从已有空间构造新空间。"积"把两个空间拼在一起（维数相加），"商"把一个子空间模掉（维数相减）。直和是积空间中"交为零"的特殊和——四种等价刻画（交为零、分解唯一、零分解唯一、维数相加）必须熟记。商空间 $V/U$ 的核心思想是将 $U$ 中的元素视为零，$\dim(V/U)=\dim V-\dim U$。线性映射基本定理（第一同构定理）：$V/\ker T\cong\operatorname{im}T$，这是商空间最重要的应用。

> **知识链**：子空间的交 $\cap$（子空间）/和 $+$（子空间）/并 $\cup$（不一定是）→ 维数公式 $\dim W_1+\dim W_2=\dim(W_1+W_2)+\dim(W_1\cap W_2)$ → 直和 $\oplus$（四等价条件）→ 覆盖定理（有限个非平凡子空间不能覆盖全空间）→ 商空间 $V/U$（模去等价类）→ $\dim(V/U)=\dim V-\dim U$ → 商映射 $\pi:V\to V/U$ → 第一同构定理 $V/\ker T\cong\operatorname{im}T$ → 积空间 $V\times W$

### 2.1 交、和、直和

**定义 2.1（交、和）** 设 $W_1,W_2$ 为 $V$ 的子空间：
- $W_1\cap W_2$ 为交（子空间）
- $W_1+W_2=\{\alpha_1+\alpha_2\mid\alpha_i\in W_i\}$ 为和（子空间）
- $W_1\cup W_2$ 一般不是子空间，除非 $W_1\subseteq W_2$ 或 $W_2\subseteq W_1$

**定理 2.1（维数公式）** 
$$\dim W_1+\dim W_2=\dim(W_1+W_2)+\dim(W_1\cap W_2).$$

**定义 2.2（直和）** 若 $W_1\cap W_2=\{0\}$，则 $W_1+W_2$ 为直和，记为 $W_1\oplus W_2$。

**定理 2.2（直和等价命题）** 以下等价：
1. $W_1+W_2$ 为直和（$W_1\cap W_2=\{0\}$）
2. 任意 $\alpha\in W_1+W_2$ 的分解 $\alpha=\alpha_1+\alpha_2$（$\alpha_i\in W_i$）唯一
3. 零向量只有平凡分解：$\alpha_1+\alpha_2=\vec{0}\Rightarrow\alpha_1=\alpha_2=\vec{0}$
4. $\dim(W_1+W_2)=\dim W_1+\dim W_2$

**定义 2.3（多空间直和）** $\sum W_i$ 为直和 $\iff$ 对每个 $i$，$W_i\cap\sum_{j\neq i}W_j=\{0\}$。

### 2.2 商空间

**定义 2.4（商空间）** 设 $U$ 为 $V$ 的子空间。定义等价关系 $\alpha\sim\beta\iff\alpha-\beta\in U$。所有等价类的集合 $V/U=\{v+U\mid v\in V\}$ 在自然定义的加法与数乘下构成线性空间。

**定理 2.3（商空间维数公式）** 
$$\dim(V/U)=\dim V-\dim U.$$

**定理 2.4（商映射的泛性质）** 商映射 $\pi:V\to V/U$，$\pi(v)=v+U$ 是线性满射，且 $\ker\pi=U$。若 $T:V\to W$ 满足 $U\subseteq\ker T$，则存在唯一 $\tilde{T}:V/U\to W$ 使 $T=\tilde{T}\circ\pi$。

**定理 2.5（第一同构定理）** 对任意线性映射 $T:V\to W$，
$$V/\ker T\cong\operatorname{im}T.$$
特别地，$\dim V-\dim\ker T=\dim\operatorname{im}T$（秩-零化度公式）。

### 2.3 积空间

**定义 2.5（积空间）** $V\times W=\{(\alpha,\beta)\mid\alpha\in V,\beta\in W\}$，加法与数乘按分量定义。$\dim(V\times W)=\dim V+\dim W$。

**定理 2.6（直和与积空间的关系）** 若 $V=W_1\oplus W_2$，则映射 $W_1\times W_2\to V$，$(\alpha_1,\alpha_2)\mapsto\alpha_1+\alpha_2$ 为同构。

**定理 2.7（覆盖定理）** 若 $V_1,\ldots,V_s$ 为 $V$ 的非平凡子空间，则 $V_1\cup\cdots\cup V_s\subsetneq V$（有限个非平凡子空间不能覆盖全空间）。

### 2.4 仿射子集

**定义 2.6（仿射子集）** 子空间 $U$ 的平移 $v+U=\{v+u\mid u\in U\}$ 称为仿射子集。

**定理 2.8（仿射子集刻画）** 非空子集 $A$ 是仿射子集 $\iff$ $\forall v,w\in A,\forall\lambda\in\mathbf{F}$，有 $\lambda v+(1-\lambda)w\in A$。

---

## 第三章 对偶空间

> **复习要点**：对偶空间是下学期最抽象的内容。核心思想：将空间中的向量转化为"如何作用在向量上"的函数。$V^*$ 是 $V$ 上所有线性泛函构成的空间，$\dim V^*=\dim V$。关键区分：$V\cong V^*$ 是非自然同构（需要选基），但 $V\cong V^{**}$ 是自然同构（$\psi(v)(f)=f(v)$，不依赖基）。零化子 $U^0=\{\varphi\in V^*\mid\varphi|_U=0\}$ 是子空间对应到对偶空间中的"正交补"：$\dim U^0=\dim V-\dim U$。对偶映射 $f^*(\varphi)=\varphi\circ f$ 是反变的（箭头方向反转），$(f\circ g)^*=g^*\circ f^*$。重要关系：$\ker\sigma^*=(\operatorname{im}\sigma)^0$，$\operatorname{im}\sigma^*=(\ker\sigma)^0$；$\sigma$ 单 $\iff\sigma^*$ 满。对偶空间把商空间和零化子联系起来：$(V/W)^*\cong W^0$。

> **知识链**：线性泛函 $f:V\to\mathbf{F}$（超平面）→ $V^*=\mathcal{L}(V,\mathbf{F})$ → 对偶基 $f_i(e_j)=\delta_{ij}$ → $V\cong V^*$（依赖基，非自然）→ $V\cong V^{**}$（自然同构，$\psi(v)(f)=f(v)$）→ 零化子 $U^0=\{\varphi\mid\varphi|_U=0\}$ → $\dim U^0=\dim V-\dim U$ → 对偶映射 $f^*(\varphi)=\varphi\circ f$（反变）→ $(f\circ g)^*=g^*\circ f^*$ → $\ker\sigma^*=(\operatorname{im}\sigma)^0$，$\operatorname{im}\sigma^*=(\ker\sigma)^0$ → $\sigma$ 单 $\iff\sigma^*$ 满 → $(V/W)^*\cong W^0$ → 公共零点集 $Z(U)=\{v\mid\forall\varphi\in U,\varphi(v)=0\}$ → $U^{00}\cong U$

### 3.1 对偶空间与对偶基

**定义 3.1（线性泛函）** 线性映射 $f:V\to\mathbf{F}$ 称为线性泛函。全体线性泛函构成 $V$ 的对偶空间 $V^*=\mathcal{L}(V,\mathbf{F})$。

**定义 3.2（对偶基）** 设 $\{e_1,\ldots,e_n\}$ 为 $V$ 的基。对偶基 $\{f_1,\ldots,f_n\}$ 由 $f_i(e_j)=\delta_{ij}$ 唯一确定。

**定理 3.1（对偶基构成基）** $\dim V^*=\dim V$，且对偶基是对偶空间 $V^*$ 的一组基。

**定理 3.2（$V\cong V^*$ 非自然）** 同构 $V\to V^*$，$e_i\mapsto f_i$ 依赖于基的选取，不是自然同构。

**定理 3.3（自然同构 $V\cong V^{**}$）** 映射 $\psi:V\to V^{**}$，$\psi(v)(\varphi)=\varphi(v)$ 是同构，且不依赖基的选取（自然同构）。交换性：$\sigma^{**}\circ\psi=\psi\circ\sigma$。

### 3.2 零化子

**定义 3.3（零化子）** 对子空间 $U\subseteq V$，$U^0=\{\varphi\in V^*\mid\forall u\in U,\ \varphi(u)=0\}$。

**定理 3.4（零化子维数）** $\dim U^0=\dim V-\dim U$。

**定理 3.5（零化子运算性质）**
1. $(U_1+U_2)^0=U_1^0\cap U_2^0$
2. $(U_1\cap U_2)^0=U_1^0+U_2^0$
3. $S^{00}=\operatorname{span}S$（其中 $S^0=\{\varphi\mid\varphi|_S=0\}$）

**定义 3.4（公共零点集）** 设 $U\subseteq V^*$，$Z(U)=\{v\in V\mid\forall\varphi\in U,\ \varphi(v)=0\}$。

**定理 3.6（对偶配对）** 若 $U\subseteq V$ 为子空间，则 $Z(U^0)=U$；若 $U\subseteq V^*$ 为子空间，则 $Z(U)^0=U$。

### 3.3 对偶映射

**定义 3.5（对偶映射）** 对 $f:V\to W$，其对偶映射 $f^*:W^*\to V^*$ 定义为 $f^*(\varphi)=\varphi\circ f$。

**定理 3.7（对偶映射的函子性）** 
1. $(f\circ g)^*=g^*\circ f^*$
2. $(\operatorname{id}_V)^*=\operatorname{id}_{V^*}$
3. 对偶映射是线性映射

**定理 3.8（对偶映射的像与核）**
1. $\ker\sigma^*=(\operatorname{im}\sigma)^0$
2. $\operatorname{im}\sigma^*=(\ker\sigma)^0$
3. $\dim\operatorname{im}\sigma^*=\dim\operatorname{im}\sigma$

**推论 3.1（单满互换）** $\sigma$ 单 $\iff\sigma^*$ 满；$\sigma$ 满 $\iff\sigma^*$ 单。

### 3.4 商空间与对偶

**定理 3.9（$(V/W)^*\cong W^0$）** 若 $W\subseteq V$，则 $(V/W)^*\cong W^0$。

**定理 3.10（纤维结构）** 对线性映射 $f:V\to W$，若 $f(v_0)=w$，则 $f^{-1}(w)=v_0+\ker f$。

**推论 3.2（线性泛函的核）** 非零线性泛函 $\varphi$ 的核是超平面（余维为1的子空间），$\dim V/(\ker\varphi)=1$。

---

## 第四章 多项式

> **复习要点**：多项式理论是若当标准形和有理标准形的代数基础。核心链条：带余除法 → 最大公因式（Euclid算法）→ 裴蜀定理（$(p,q)=1\iff\exists u,v,up+vq=1$）→ 不可约多项式 → 唯一分解定理。**重因式判别**（$p$ 无重因式 $\iff(p,p')=1$）是后续最小多项式理论的关键引理。**代数学基本定理**：复多项式必有根 → 复数域不可约多项式皆为一次；实数域不可约多项式为一次或判别式$<0$的二次。关于 $\mathbf{Q}$ 上的不可约性：有理根检验（$p/q$ 根→ $p\mid a_n,q\mid a_0$）、高斯引理、艾森斯坦判别法。**多项式商环** $\mathbf{F}[x]/(p(x))$ 维数 $=\deg p$，基为 $\{1+(p),\ldots,x^{n-1}+(p)\}$——这个结构在循环子空间理论中反复出现。

> **知识链**：多项式次数 → 整除 $q\mid p$ → 带余除法 $p=sq+r$（$\deg r<\deg s$）→ gcd（Euclid算法）→ 裴蜀定理（互素 $\iff$ 线性组合 $=1$）→ 不可约多项式（素性）→ 唯一分解定理 → 形式导数 → 重因式 $p$ 无重因式 $\iff(p,p')=1$ → 根 $\iff(x-\lambda)\mid p$ → 代数学基本定理（$\mathbf{C}$ 上必有一根）→ 韦达定理 → 有理根检验 → 高斯引理 → 艾森斯坦判据 → $\dim\mathbf{F}[x]/(p)=\deg p$

### 4.1 整除与带余除法

**定义 4.1（多项式与次数）** 数域 $\mathbf{F}$ 上的多项式 $p(x)=a_0+a_1x+\cdots+a_mx^m$。若 $a_m\neq0$，则称 $\deg p=m$，$a_m$ 为首项系数（$a_m=1$ 时称首一多项式）。零多项式次数无定义。

**定理 4.1（次数性质）** $\deg(p+q)\le\max\{\deg p,\deg q\}$，$\deg(kp)=\deg p$（$k\neq0$），$\deg(pq)=\deg p+\deg q$。

**定理 4.2（带余除法）** 对 $p,s\in\mathbf{F}[x]$，$s\neq0$，存在唯一 $q,r$ 使 $p=sq+r$ 且 $\deg r<\deg s$。

**定义 4.2（整除）** 若 $p=sq$，则称 $q\mid p$。$p\mid q$ 且 $q\mid p$ $\iff$ $p=kq$（$k\neq0$），此时称 $p\sim q$（相伴）。

### 4.2 最大公因式与互素

**定义 4.3（最大公因式）** 首一次数最高的公因式，记为 $(p,q)$。

**定理 4.3（Euclid算法与裴蜀定理）**
1. $(p,q)$ 存在，且可通过辗转相除法求得
2. $\exists u,v\in\mathbf{F}[x]$，使得 $(p,q)=up+vq$

**定理 4.4（裴蜀定理——互素等价）** $(p,q)=1\iff\exists u,v\in\mathbf{F}[x],\ up+vq=1$。

**定理 4.5（互素性质）** 若 $(p,q)=1$ 且 $p\mid qs$，则 $p\mid s$。

**定理 4.6（中国剩余定理）** 若 $q_i$ 两两互素，则 $p\equiv r_i\pmod{q_i}$ 在模 $\prod q_i$ 下有唯一解。

### 4.3 不可约与唯一分解

**定义 4.4（不可约多项式）** 非常数多项式若不能分解为两个次数更低的多项式之积，则称为不可约。

**定理 4.7（不可约多项式的素性）** 若 $p$ 不可约，则 $p$ 要么整除 $q$ 要么与 $q$ 互素；若 $p\mid q_1q_2$，则 $p\mid q_1$ 或 $p\mid q_2$。

**定理 4.8（唯一分解定理）** 任意非常数多项式可唯一分解为不可约多项式之积（在相伴与顺序意义下）。

### 4.4 重因式与形式导数

**定义 4.5（形式导数）** $p'(x)=a_1+2a_2x+\cdots+na_nx^{n-1}$。

**定理 4.9（重因式判别）** $p$ 无重因式 $\iff(p,p')=1$。若 $d=(p,p')$，则 $p/d$ 无重因式且保留全部不可约因子（不计重数）。

### 4.5 根与代数学基本定理

**定理 4.10（根与因式）** $p(\lambda)=0\iff(x-\lambda)\mid p$。$m$ 次多项式至多有 $m$ 个互异根。

**定理 4.11（代数学基本定理）** 任意非常数复多项式在 $\mathbf{C}$ 上至少有一个根。

**推论 4.1（复数域分解）** $\mathbf{C}$ 上不可约多项式皆为一次；$n$ 次多项式恰有 $n$ 个复根（计重数）。

**定理 4.12（实数域分解）** 实系数多项式可分解为一次因子与不可约二次因子之积。实系数多项式若有非实复根，则其共轭也是根。

### 4.6 有理数域上的不可约性

**定理 4.13（有理根检验）** 若整系数多项式有有理根 $q/p$（既约），则 $p\mid a_n$，$q\mid a_0$。

**定理 4.14（高斯引理）** 本原多项式（整系数且系数互素）之积仍为本原多项式。

**定理 4.15（艾森斯坦判别法）** 若存在素数 $p$ 使 $p\mid a_0,\ldots,a_{n-1}$，$p\nmid a_n$，$p^2\nmid a_0$，则多项式在 $\mathbf{Q}$ 上不可约。

### 4.7 多项式理想与商空间

**定理 4.16（商空间维数）** $\dim\mathbf{F}[x]/(p(x))=\deg p$，基可取 $\{1+(p),x+(p),\ldots,x^{n-1}+(p)\}$。

---

## 第五章 不变子空间、本征值与本征向量

> **复习要点**：本章是整个相似标准形理论的基石。核心概念链：相似 $B=P^{-1}AP$（同一变换在不同基下）→ 不变子空间（$\sigma(U)\subseteq U$，将变换限制在子空间内）→ 若 $V$ 分解为不变子空间直和，则矩阵分块对角化 → 一维不变子空间 $\iff$ 特征向量。特征多项式 $\chi_A(t)=\det(tI-A)$，系数由主子式和给出。**代数重数 $\ge$ 几何重数** 是核心不等式。可对角化的五个等价条件（存在特征基 / 直和分解 / 维数和 / 代重=几重 / 最小多项式无重根）必须熟记。复空间总有特征值 → 总可上三角化。幂等矩阵就是投影，幂零矩阵等价于所有特征值为零。

> **知识链**：相似 $\sim$ → 不变子空间 → 限制/商变换 → 分块对角 $\iff$ 直和不变 → 特征值/特征向量 $\sigma(v)=\lambda v$ → 特征子空间 $E_\lambda=\ker(\sigma-\lambda I)$ → 特征多项式 $\chi_A(t)=\det(tI-A)$ → 系数（迹/行列式/主子式和）→ $\chi_A=\chi_B$ 相似不变 → 互异特征值 → 特征向量线性无关 → 代数重数$\ge$几何重数 → 可对角化（5等价条件）→ 上三角化（复空间总可以）→ 幂等（投影）/幂零→ Cayley-Hamilton $\chi_A(A)=0$ → 最小多项式 $m_A(t)$ → $m_A$ 无重根 $\iff$ 可对角化

### 5.1 相似与不变子空间

**定义 5.1（相似）** $A,B\in\mathbf{M}_n(\mathbf{F})$ 称为相似，若存在可逆 $P$ 使 $B=P^{-1}AP$。同一线性变换在不同基下的矩阵相似。

**定义 5.2（不变子空间）** 子空间 $U\subseteq V$ 在 $\sigma$ 下不变，若 $\sigma(U)\subseteq U$。此时可定义**限制变换** $\sigma|_U\in\mathcal{L}(U)$，和**商变换** $\sigma/U\in\mathcal{L}(V/U)$。

**定理 5.1（分块对角 $\iff$ 不变直和）** $V=U_1\oplus\cdots\oplus U_m$，则 $\sigma$ 在拼接基下的矩阵为 $\operatorname{diag}(A_1,\ldots,A_m)$ $\iff$ 每个 $U_i$ 均在 $\sigma$ 下不变。

**定义 5.3（循环子空间）** 对 $v\neq0$，$W=\operatorname{span}(v,\sigma v,\sigma^2v,\ldots)$ 是包含 $v$ 的最小 $\sigma$-不变子空间。若 $\dim W=m$，则 $\{v,\sigma v,\ldots,\sigma^{m-1}v\}$ 是 $W$ 的循环基。

### 5.2 特征值与特征向量

**定义 5.4（特征值、特征向量、特征子空间）** 若存在非零 $v$ 使 $\sigma(v)=\lambda v$，则 $\lambda$ 为特征值，$v$ 为特征向量。$E_\lambda=\ker(\sigma-\lambda I)$ 为特征子空间。

**定义 5.5（特征多项式）** $\chi_A(t)=\det(tI-A)=t^n+a_1t^{n-1}+\cdots+a_n$。

**定理 5.2（特征多项式系数公式）** $a_1=-\operatorname{tr}(A)$，$a_n=(-1)^n\det(A)$，$a_k=(-1)^k\sum$(所有 $k$ 阶主子式之和)。

**定理 5.3（特征值基本性质）**
1. 特征值是 $\det(tI-A)=0$ 的根
2. 互异特征值对应的特征向量线性无关
3. $\lambda$ 是 $\sigma^m$ 的特征值；若 $A$ 可逆，$\lambda^{-1}$ 是 $A^{-1}$ 的特征值
4. 对任意多项式 $f$，$f(\lambda)$ 是 $f(\sigma)$ 的特征值
5. $A$ 与 $A^\mathsf{T}$ 特征值相同

**定理 5.4（代数重数与几何重数）** 代数重数 $a_\lambda=$ $\lambda$ 在 $\chi$ 中的重数；几何重数 $g_\lambda=\dim E_\lambda$。总有 $1\le g_\lambda\le a_\lambda$。

**定理 5.5（Gershgorin圆盘）** 所有特征值落在 $\bigcup_i\{z:|z-a_{ii}|\le\sum_{j\neq i}|a_{ij}|\}$ 中。

### 5.3 对角化

**定义 5.6（可对角化）** 若 $A$ 相似于对角矩阵，则称 $A$ 可对角化。

**定理 5.6（可对角化等价条件）** 以下等价：
1. $\sigma$ 可对角化
2. $V$ 有一组由特征向量构成的基
3. $V=\bigoplus_{i=1}^s E_{\lambda_i}$
4. $\sum_i\dim E_{\lambda_i}=\dim V$
5. 每个特征值的代数重数等于几何重数
6. 最小多项式无重根（见§5.5）

**定理 5.7（同时对角化）** 若 $A,B$ 均可对角化且 $AB=BA$，则存在同一 $P$ 使 $P^{-1}AP$ 和 $P^{-1}BP$ 同时为对角矩阵。

### 5.4 上三角化与特殊算子

**定理 5.8（复空间上三角化）** 任意复线性空间上的线性变换都可上三角化。上三角矩阵的对角元恰为其特征值（计重数）。

**定理 5.9（可交换算子）** 若 $AB=BA$，则 $A$ 与 $B$ 有公共特征向量；在复空间上可同时上三角化。

**定理 5.10（幂等 $\iff$ 投影）** $P^2=P$ $\iff$ $P$ 是到 $\operatorname{im}P$ 的投影（沿 $\ker P$）。幂等矩阵特征值仅 $0$ 和 $1$，$\operatorname{tr}(P)=r(P)$。

**定理 5.11（幂零等价条件）** 对 $N\in\mathcal{L}(V)$，以下等价：
1. $N^k=0$（某 $k$）
2. 所有特征值为 $0$
3. 在某组基下矩阵为严格上三角

### 5.5 Cayley-Hamilton与最小多项式

**定理 5.12（Cayley-Hamilton）** 任意矩阵满足其特征多项式：$\chi_A(A)=0$。

**定义 5.7（最小多项式）** $A$ 的最小多项式 $m_A(t)$ 是首一次数最低的满足 $m_A(A)=0$ 的多项式。

**定理 5.13（最小多项式基本性质）**
1. $m_A\mid\chi_A$，且两者有相同的根
2. 对任意多项式 $p$，$p(A)=0\iff m_A\mid p$
3. 相似矩阵最小多项式相同
4. $m_A$ 无重根 $\iff A$ 可对角化
5. $m_A$ 中 $(t-\lambda)$ 的指数 $=$ $\lambda$ 的最大若当块阶数

**定理 5.14（互素分解引理）** 若 $p=p_1p_2$ 且 $(p_1,p_2)=1$，则 $\ker p(\sigma)=\ker p_1(\sigma)\oplus\ker p_2(\sigma)$。更一般地，若 $p=\prod p_i$ 两两互素，则 $\ker p(\sigma)=\bigoplus_i\ker p_i(\sigma)$。

**推论 5.1（广义特征分解）** 设 $\chi_\sigma(t)=\prod_i(t-\lambda_i)^{r_i}$，则 $V=\bigoplus_i\ker(\sigma-\lambda_iI)^{r_i}$。

---

## 第六章 内积空间

> **复习要点**：内积空间 = 线性空间 + 内积（满足正定性、共轭对称、对前一个变元线性）。几何直觉完全成立：Cauchy-Schwarz 不等式 $|\langle u,v\rangle|\le\|u\|\|v\|$ 保证了夹角的定义（$|\cos\theta|\le1$）；Gram-Schmidt 正交化将任意基变为标准正交基（每个有限维内积空间都有标准正交基）；正交补给出直和分解 $V=U\oplus U^\perp$。Riesz 表示定理：在有限维内积空间中，每个线性泛函 $f$ 唯一对应一个向量 $y$，使得 $f(x)=\langle x,y\rangle$——这建立了 $V$ 与 $V^*$ 的自然（依赖于内积而非基）同构。正交投影 $\operatorname{proj}_U v$ 是 $v$ 在子空间 $U$ 中的"最佳逼近"。Bessel 不等式（$\sum|\langle v,e_j\rangle|^2\le\|v\|^2$）和 Parseval 等式（标准正交基时等号成立）是 Fourier 分析的基础。Schur 定理：复内积空间中任何算子可酉上三角化。QR 分解：可逆矩阵 $A=QR$（$Q$ 酉，$R$ 上三角且对角元为正），唯一。

> **知识链**：内积（正定+共轭对称+线性）→ 范数 $\|v\|=\sqrt{\langle v,v\rangle}$ → Cauchy-Schwarz → 三角不等式 → 极化恒等式 → 正交 $\perp$ → 标准正交组（线性无关）→ Gram-Schmidt 正交化 → 标准正交基存在 → 正交补 $U^\perp$（$V=U\oplus U^\perp$）→ Riesz 表示 $f(x)=\langle x,y\rangle$ → 正交投影 $P_U$（$\|v-P_Uv\|=\min$）→ 最佳逼近 / 最小二乘（$A^*Ax=A^*b$）→ Fourier 展开 $\sum\langle v,e_i\rangle e_i$ → Bessel 不等式 → Parseval 等式 → Gram 矩阵 $G_{ij}=\langle e_i,e_j\rangle$ → QR 分解 → Schur 上三角化

### 6.1 内积与基本性质

**定义 6.1（内积空间）** 线性空间 $V$ 上的内积 $\langle\cdot,\cdot\rangle:V\times V\to\mathbf{F}$ 满足：
1. 正定性：$\langle v,v\rangle\ge0$，等号 $\iff v=0$
2. 共轭对称：$\langle u,v\rangle=\overline{\langle v,u\rangle}$（实情形为对称）
3. 线性于第一变元：$\langle\lambda u+v,w\rangle=\lambda\langle u,w\rangle+\langle v,w\rangle$

**定义 6.2（范数与正交）** $\|v\|=\sqrt{\langle v,v\rangle}$；若 $\langle u,v\rangle=0$，称 $u\perp v$。

**定理 6.1（Cauchy-Schwarz不等式）** $|\langle u,v\rangle|\le\|u\|\|v\|$，等号 $\iff u,v$ 线性相关。

**推论 6.1（三角不等式）** $\|u+v\|\le\|u\|+\|v\|$。

**定理 6.2（极化恒等式）** 实内积：$\langle u,v\rangle=\frac14(\|u+v\|^2-\|u-v\|^2)$。

**定理 6.3（勾股定理）** 若 $u\perp v$，则 $\|u+v\|^2=\|u\|^2+\|v\|^2$。

### 6.2 标准正交基与Gram-Schmidt

**定义 6.3（标准正交组/基）** 两两正交且范数均为 $1$ 的向量组。标准正交组必线性无关。

**定理 6.4（Gram-Schmidt正交化）** 任意线性无关组可正交化为标准正交组。特别地，每个有限维内积空间都有标准正交基。

**定理 6.5（Fourier展开）** 若 $\{e_i\}$ 为标准正交基，则 $v=\sum\langle v,e_i\rangle e_i$，且 $\|v\|^2=\sum|\langle v,e_i\rangle|^2$。

**定理 6.6（QR分解）** 可逆矩阵 $A$ 可唯一分解为 $A=QR$，其中 $Q$ 的列构成标准正交基，$R$ 为上三角且对角元全正。

**定理 6.7（Schur定理）** 复内积空间上任意线性变换可相对于某标准正交基有上三角矩阵（即酉上三角化）。

### 6.3 正交补与正交投影

**定义 6.4（正交补）** $U^\perp=\{v\in V\mid\forall u\in U,\langle v,u\rangle=0\}$。

**定理 6.8（正交补分解）** 若 $V$ 有限维，则 $V=U\oplus U^\perp$，$\dim U^\perp=\dim V-\dim U$，且 $(U^\perp)^\perp=U$。

**定义 6.5（正交投影）** $\operatorname{proj}_U v=u$，其中 $v=u+w$，$u\in U$，$w\in U^\perp$。

**定理 6.9（最佳逼近）** $\operatorname{proj}_U v$ 是 $U$ 中到 $v$ 的最近点：$\|v-\operatorname{proj}_U v\|=\min_{u\in U}\|v-u\|$。

**定理 6.10（Riesz表示定理）** 在有限维内积空间中，对每个线性泛函 $f\in V^*$，存在唯一 $y\in V$ 使 $f(x)=\langle x,y\rangle$（$\forall x\in V$）。映射 $y\mapsto\langle\cdot,y\rangle$ 是 $V$ 到 $V^*$ 的（依赖于内积的）同构。

### 6.4 应用与补充定理

**定理 6.11（最小二乘法）** 超定方程组 $Ax\approx b$ 的最小二乘解满足正规方程 $A^*Ax=A^*b$。

**定义 6.6（Gram矩阵）** $G_{ij}=\langle e_i,e_j\rangle$。向量组线性无关 $\iff\det G\neq0$。

**定理 6.12（Bessel不等式）** 若 $\{e_1,\ldots,e_m\}$ 为标准正交组，则 $\sum_{j=1}^m|\langle v,e_j\rangle|^2\le\|v\|^2$，等号 $\iff v\in\operatorname{span}\{e_j\}$。

**定理 6.13（Parseval等式）** 若 $\{e_i\}$ 为标准正交**基**，则 $\|v\|^2=\sum|\langle v,e_i\rangle|^2$。

---

## 第七章 内积空间上的算子

> **复习要点**：本章是内积空间理论的制高点——以**伴随算子** $T^*$ 为核心，建立内积空间上算子的完整分类体系。伴随算子由 $\langle Tx,y\rangle=\langle x,T^*y\rangle$ 定义，是内积赋予每个算子的"对偶"（在标准正交基下矩阵为共轭转置 $A^H$），由此一切算子按"与自身伴随的关系"形成递进分类：一般算子 $\supset$ 正规算子（$T^*T=TT^*$，与伴随交换）$\supset$ 自伴算子（$T^*=T$，Hermitian，特征值全实数）/ 酉或正交变换（$T^{-1}=T^*$，保积同构）$\supset$ 正算子（$\langle Tv,v\rangle\ge0$，5个等价刻画）。两个核心谱定理是整套理论的终点：**实谱定理**——实空间上 $T$ 自伴 $\iff$ 可正交对角化；**复谱定理**——复空间上 $T$ 正规 $\iff$ 可酉对角化。任意复算子可唯一写为 $T=T_1+iT_2$（$T_1,T_2$ 自伴），且 $T$ 正规 $\iff T_1T_2=T_2T_1$——这类似于复数 $z=x+iy$ 的实部虚部分解。正算子的唯一正平方根 $\sqrt{T}$ 直接导出第8章的极分解 $T=U\sqrt{T^*T}$ 和奇异值分解——因此本章是内积空间算子理论的枢纽：上承第6章的正交投影与Riesz表示（投影是自伴幂等算子；Riesz表示是伴随在泛函层面的特例），下启第8章（$\sqrt{T^*T}$ 来自正算子理论，谱定理是 SVD 展开式的理论基础）。第10章将复谱定理的结论推广到实空间，处理实正规算子的 $2\times2$ 旋转块结构。

> **知识链**：伴随 $T^*$（$\langle Tx,y\rangle=\langle x,T^*y\rangle$，内积赋予每个算子的"影子"）→ 矩阵 $A^H=\overline{A^\mathsf{T}}$ → $\ker T^*=(\operatorname{im}T)^\perp$，$\operatorname{im}T^*=(\ker T)^\perp$（伴随与正交补的精确对偶）→ 算子分类：按 $T$ 与 $T^*$ 的关系递进 ↓
> - 正规算子 $T^*T=TT^*$：与伴随交换，$\|Tv\|=\|T^*v\|$，不同特征值特征向量正交，不变子空间 $\Rightarrow$ 正交补也不变
>   - 自伴算子 $T^*=T$：Hermitian，特征值全实数，$\langle Tv,v\rangle\in\mathbf{R}$，实谱定理（自伴 $\iff$ 可正交对角化）
>   - 酉/正交变换 $T^{-1}=T^*$：保积同构，特征值模为 $1$，将标准正交基映为标准正交基
>   - 正算子 $T\ge0$：5等价刻画（自伴+特征值 $\ge0$ / 有正平方根 / 有自伴平方根 / $T=R^*R$），正平方根唯一
> → 复谱定理：复空间上 $T$ 正规 $\iff$ 可酉对角化（内积空间算子理论的最高成就）
> → 算子分解 $T=T_1+iT_2$（$T_1,T_2$ 自伴），$T$ 正规 $\iff T_1T_2=T_2T_1$（类比复数实虚部）
> → 同时酉对角化（交换的正规算子族可同时对角化）
> → 导出第8章：正平方根 $\sqrt{T^*T}$ → 极分解 $T=U\sqrt{T^*T}$ → 奇异值 $\sigma_i=\sqrt{\lambda_i(T^*T)}$ → SVD
> → 导出第10章：实正规算子的结构定理（$1\times1$ 块 + $2\times2$ 旋转块）

### 7.1 伴随算子

**定义 7.1（伴随算子）** 设 $T\in\mathcal{L}(V,W)$（$V,W$ 为内积空间）。$T^*\in\mathcal{L}(W,V)$ 由 $\langle Tx,y\rangle_W=\langle x,T^*y\rangle_V$ 唯一定义。

**定理 7.1（伴随基本性质）**
1. $(S+T)^*=S^*+T^*$，$(\lambda T)^*=\overline{\lambda}T^*$
2. $(ST)^*=T^*S^*$（反序）
3. $(T^*)^*=T$
4. 若 $T$ 可逆，则 $(T^{-1})^*=(T^*)^{-1}$

**定理 7.2（伴随的矩阵表示）** 在标准正交基下，$T^*$ 的矩阵是 $T$ 的矩阵的共轭转置 $A^H=\overline{A^\mathsf{T}}$。

**定理 7.3（伴随的核与像）**
- $\ker T^*=(\operatorname{im}T)^\perp$，$\operatorname{im}T^*=(\ker T)^\perp$
- $\ker T=(\operatorname{im}T^*)^\perp$，$\operatorname{im}T=(\ker T^*)^\perp$

### 7.2 自伴算子与实谱定理

**定义 7.2（自伴算子）** $T^*=T$。实内积空间中称对称算子，复内积空间中称 Hermitian 算子。

**定理 7.4（自伴性质）** 自伴算子的特征值全为实数；不同特征值的特征向量正交。

**定理 7.5（实谱定理——自伴算子的对角化）** 实内积空间上 $T$ 自伴 $\iff$ $V$ 有一组由 $T$ 的特征向量构成的标准正交基 $\iff$ $T$ 可正交对角化。

**定理 7.6（自伴算子补充刻画）** $T$ 自伴 $\iff\langle Tv,v\rangle\in\mathbf{R}$（对所有 $v$）。若 $T$ 自伴且 $\langle Tv,v\rangle=0$（对所有 $v$），则 $T=0$。

### 7.3 正交/酉变换

**定义 7.3（正交变换 / 酉变换）** $T^*T=TT^*=I$，即 $T^{-1}=T^*$。实空间称正交变换，复空间称酉变换。等价于保积同构。

**定理 7.7（正交/酉变换刻画）** 以下等价：$T$ 是正交（酉）变换；$T$ 将某组标准正交基映为标准正交基；$T$ 可逆且 $T^{-1}=T^*$。正交/酉矩阵的特征值模为 $1$。

### 7.4 正规算子与复谱定理

**定义 7.4（正规算子）** $T^*T=TT^*$。

自伴算子、酉/正交算子均为正规算子。正规算子包含更一般的"可对角化 + 正交特征基"的情形。

**定理 7.8（正规算子的等价条件）** $T$ 正规 $\iff\|Tv\|=\|T^*v\|$（对所有 $v$）。

**定理 7.9（正规算子的关键性质）**
1. 若 $T$ 正规，则不同特征值的特征向量正交
2. 若 $T$ 正规且 $U$ 在 $T$ 下不变，则 $U^\perp$ 也在 $T$ 下不变，且 $T|_U$ 和 $T|_{U^\perp}$ 均为正规的

**定理 7.10（复谱定理——正规算子的对角化）** 复内积空间上 $T$ 正规 $\iff$ $V$ 有一组由 $T$ 的特征向量构成的标准正交基 $\iff$ $T$ 可酉对角化。

**定理 7.11（正规算子的分解）** 任意 $T\in\mathcal{L}(V)$ 可唯一写为 $T=T_1+iT_2$，其中 $T_1=\frac{T+T^*}{2}$，$T_2=\frac{T-T^*}{2i}$ 均为自伴。且 $T$ 正规 $\iff T_1T_2=T_2T_1$。

**定理 7.12（同时酉对角化）** 若 $T_1,T_2$ 均为正规算子且可交换，则它们可同时酉对角化。

### 7.5 正算子

**定义 7.5（正算子）** $T\ge0$ 若对任意 $v$，$\langle Tv,v\rangle\ge0$（等价于 $T$ 自伴且特征值 $\ge0$）。

**定理 7.13（正算子等价刻画）** 以下等价：
1. $T$ 是正算子
2. $T$ 自伴且所有特征值 $\ge0$
3. $T$ 有正平方根（存在唯一 $R\ge0$ 使 $R^2=T$）
4. $T$ 有自伴平方根
5. 存在 $R$ 使 $T=R^*R$

---

## 第八章 奇异值与极分解

> **复习要点**：极分解和奇异值分解（SVD）将线性映射分解为"旋转-拉伸-旋转"，是矩阵分析的终极工具。极分解 $T=U\sqrt{T^*T}$ 类似复数极坐标 $z=re^{i\theta}$——$U$ 是部分等距（旋转），$P=\sqrt{T^*T}$ 是正算子（拉伸）。奇异值 $\sigma_i=\sqrt{\lambda_i(T^*T)}$ 是拉伸因子（总是非负实数）。SVD $A=U\Sigma V^*$ 将任意矩阵分解为两个酉矩阵和一个非负对角矩阵的乘积。推论：矩阵秩 = 非零奇异值个数；算子2-范数 = 最大奇异值；截断SVD给出最优低秩逼近。

> **知识链**：正平方根 $R=\sqrt{T^*T}$（唯一正算子）→ 极分解 $T=U\sqrt{T^*T}$ → 奇异值 $\sigma_i=\sqrt{\lambda_i(T^*T)}$ → SVD $A=U\Sigma V^*$ → 秩 = 非零奇异值个数 → $\|A\|_2=\sigma_{\max}$ → 最优低秩近似（截断SVD）→ 奇异值与单/满射的关系

### 8.1 极分解

**定理 8.1（极分解）** 任意线性算子 $T\in\mathcal{L}(V,W)$ 可分解为
$$T=UP,$$
其中 $P=\sqrt{T^*T}\ge0$ 是正算子，$U$ 是部分等距（$\|Ux\|=\|x\|$ 对 $x\in(\ker U)^\perp$）。若 $T$ 可逆，则 $U$ 为酉（实情形为正交）变换。

**注**：极分解的"唯一性"——$P$ 唯一确定（$P=\sqrt{T^*T}$），$U$ 在 $\operatorname{im}P$ 上唯一。

### 8.2 奇异值

**定义 8.1（奇异值）** $T$ 的奇异值 $\sigma_i=\sqrt{\lambda_i(T^*T)}\ge0$，其中 $\lambda_i(T^*T)$ 是正算子 $T^*T$ 的特征值。

**定理 8.2（奇异值基本性质）**
- $T$ 是单射 $\iff$ 所有奇异值 $>0$
- $T$ 是满射 $\iff$ 正奇异值个数 $=\dim W$
- 正奇异值的个数 $=\operatorname{rk}T$

### 8.3 奇异值分解（SVD）

**定理 8.3（SVD）** 对任意矩阵 $A\in\mathbf{F}^{m\times n}$，存在酉（正交）矩阵 $U\in\mathbf{M}_m$、$V\in\mathbf{M}_n$，使
$$A=U\Sigma V^*,$$
其中 $\Sigma=\operatorname{diag}(\sigma_1,\ldots,\sigma_r,0,\ldots,0)$，$\sigma_1\ge\cdots\ge\sigma_r>0$ 为 $A$ 的正奇异值。

**定理 8.4（SVD展开式）** $Ax=\sum_{i=1}^r\sigma_i\langle x,v_i\rangle u_i$，其中 $u_i,v_i$ 分别为 $U,V$ 的列（左/右奇异向量）。

**推论 8.1（秩与范数）**
1. $\operatorname{rk}A=$ 非零奇异值个数
2. $\|A\|_2=\sigma_{\max}(A)$
3. $\|A\|_F^2=\sum\sigma_i^2$

**方法 8.1（最优低秩近似）** 截断SVD保留前 $k$ 个最大奇异值，得到最优秩-$k$ 逼近（Eckart-Young 定理）。

### 8.4 补充定理

**定理 8.5（$T^*T$ 的基本性质）** $T^*T$ 是正算子；$\ker(T^*T)=\ker T$；$\operatorname{im}(T^*T)=\operatorname{im}T^*$。

**定义 8.2（奇异向量）** 若 $Tv=\sigma w$，$T^*w=\sigma v$，则 $v$ 为右奇异向量（$T^*T$ 的特征向量），$w$ 为左奇异向量（$TT^*$ 的特征向量）。

---

## 第九章 若当标准型

> **复习要点**：若当标准型是复数域上最细的相似分类，将空间分解为广义特征子空间的直和，每个广义特征子空间再分解为若当块对应的循环子空间。若当块 $J_r(\lambda)$ 是对角为 $\lambda$、上次对角为 $1$ 的 $r\times r$ 矩阵。核心工具是**块计数公式**：记 $N=A-\lambda I$，则 $\lambda$ 的若当块总数 $=\dim\ker N$；尺寸 $\ge k$ 的块数 $=\dim\ker N^k-\dim\ker N^{k-1}$。通过计数可完全确定若当标准型。**Jordan-Chevalley分解** $A=S+N$（$S$ 可对角化 + $N$ 幂零 + $SN=NS$，且均为 $A$ 的多项式）是若当理论的算子版本。最小多项式 $m_A$ 中 $(t-\lambda)$ 的指数 $=$ 最大若当块阶数，因此 $m_A$ 无重根 $\iff$ 可对角化。实矩阵在实数域上有**实若当形**（$1\times1$ 实块 + $2\times2$ 旋转伸缩块）。有理标准形在任意域上存在，代数闭域上退化为若当形；相似判定：不变因子组相同 $\iff$ 初等因子组相同。

> **知识链**：若当块 $J_r(\lambda)$ → 广义特征子空间 $G_\lambda=\ker(A-\lambda I)^{\dim V}$ → 广义特征直和分解 $\mathbf{C}^n=\bigoplus G_{\lambda_i}$ → 若当基与若当标准型（存在唯一，块顺序不计）→ 块计数公式（$\dim\ker N^k$ 的差分）→ Jordan-Chevalley分解 $A=S+N$ → 最小多项式 $m_A$（$(t-\lambda)$ 指数 = 最大块阶数）→ $m_A$ 无重根 $\iff$ 可对角化 → 实若当形（$2\times2$ 旋转块）→ 初等因子 $(\lambda-\lambda_i)^{k_{ij}}$ → 不变因子（整除链）→ 相似判定（不变因子相同 $\iff$ 初等因子组相同）

### 9.1 若当块与广义特征子空间

**定义 9.1（若当块）**
$$
J_r(\lambda)=\begin{pmatrix}
\lambda&1&&\\
&\lambda&\ddots&\\
&&\ddots&1\\
&&&\lambda
\end{pmatrix}_{r\times r}.
$$

**定义 9.2（若当标准型与若当基）** 若线性变换 $\sigma$ 在基 $B$ 下的矩阵为对角块均为若当块的分块对角矩阵，则称该矩阵为 $\sigma$ 的若当标准型，$B$ 为若当基。

**定义 9.3（广义特征子空间）** 对特征值 $\lambda$，$G_\lambda=\ker(A-\lambda I)^{\dim V}=\{v\mid\exists k,(A-\lambda I)^kv=0\}$。

**定理 9.1（广义特征直和分解）** 对复矩阵 $A$，若互异特征值为 $\lambda_1,\ldots,\lambda_s$，则
$$\mathbf{C}^n=G_{\lambda_1}\oplus\cdots\oplus G_{\lambda_s}.$$
且 $\dim G_{\lambda_i}$ 等于 $\lambda_i$ 的代数重数。

**定理 9.2（若当标准型存在性）** 任意复方阵都相似于若当标准型（若干若当块的分块对角）。

**定理 9.3（若当标准型唯一性）** 若当标准型在"每个特征值对应各块大小的多重集合"意义下唯一（块排列顺序不计）。

### 9.2 块计数公式

**定理 9.4（若当块计数公式——核心）** 固定特征值 $\lambda$，记 $N=A-\lambda I$，则：
1. $\lambda$ 的若当块总数 $=\dim\ker N$（即几何重数）。
2. 尺寸至少为 $k$ 的块数 $=\dim\ker N^k-\dim\ker N^{k-1}$。
3. 尺寸恰为 $k$ 的块数
$$=(\dim\ker N^k-\dim\ker N^{k-1})-(\dim\ker N^{k+1}-\dim\ker N^k).$$

**定理 9.5（唯一性的精确公式）** 设 $A$ 为 $n$ 阶复矩阵，$\lambda_j$ 为特征值，$N_j=A-\lambda_jI$，则：
- 主对角元为 $\lambda_j$ 的若当块个数 $n_j=n-\operatorname{rk}(N_j)$；
- $t$ 级若当块 $J_t(\lambda_j)$ 的个数为
$$n_j(t)=\operatorname{rk}(N_j^{t+1})+\operatorname{rk}(N_j^{t-1})-2\operatorname{rk}(N_j^t).$$

### 9.3 结构定理与应用

**定理 9.6（Jordan-Chevalley分解）** 任意复矩阵可唯一分解为
$$A=S+N,$$
其中 $S$ 可对角化、$N$ 幂零、$SN=NS$，且 $S,N$ 都是 $A$ 的多项式。

**定理 9.7（若当块的不变子空间）** 单个若当块 $J_n(\lambda)$ 的不变子空间恰为
$$\{0\},\;\operatorname{span}(e_1),\;\operatorname{span}(e_1,e_2),\;\ldots,\;\operatorname{span}(e_1,\ldots,e_n).$$

**定理 9.8（实数域上的实若当形）** 实矩阵在实数域上相似于由实若当块（$1\times1$ 实特征值）与 $2\times2$ 旋转伸缩块（对应共轭复特征值对 $a\pm bi$，块形如 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$ 在"上次对角"位置加 $I_2$）组成的分块对角矩阵。

**定理 9.9（矩阵幂收敛判别）** $A^m$ 收敛当且仅当每个若当块满足：$|\lambda|<1$；或该块恰是 $J_1(1)$。

### 9.4 与最小多项式的联系

**定理 9.10（最小多项式与若当块）** 设 $m_A$ 为 $A$ 的最小多项式，则对每个特征值 $\lambda$，$m_A$ 中 $(t-\lambda)$ 的指数恰等于 $A$ 的若当标准型中以 $\lambda$ 为对角元的若当块的最大阶数。

**推论 9.1（对角化判据）** $A$ 可对角化 $\iff m_A$ 在基域上分裂且无重根 $\iff$ 所有若当块均为 $1\times1$。

**定理 9.11（广义特征子空间的进一步分解）** 设 $m_A(t)=\prod(t-\lambda_i)^{r_i}$，则 $G_{\lambda_i}=\ker(A-\lambda_iI)^{r_i}$，且此分解是最细的广义特征分解。

### 9.5 初等因子、不变因子与相似判定

**定义 9.4（初等因子）** 在若当标准型中，每个若当块 $J_k(\lambda)$ 对应初等因子 $(\lambda-\lambda_i)^k$（特征值 $\lambda_i$ 对应的不可约多项式幂）。

**定义 9.5（不变因子组）** 将初等因子按不可约多项式分组并降序排列，对齐后逐列相乘得到 $d_1\mid d_2\mid\cdots\mid d_s$，称为不变因子组（整除链）。

**定理 9.12（相似判定——初等因子）** 两复矩阵相似 $\iff$ 有相同的初等因子组（即相同的若当标准型，块顺序不计）。

**定理 9.13（相似判定——不变因子）** 两矩阵（任意域上）相似 $\iff$ 有相同的不变因子组。

**定理 9.14（相似域不变性）** 设 $A,B$ 为域 $\mathbf{F}$ 上方阵，$\mathbf{K}$ 是 $\mathbf{F}$ 的扩域，则 $A,B$ 在 $\mathbf{K}$ 上相似 $\iff$ 在 $\mathbf{F}$ 上相似。

---

## 第十章 正规算子在实空间的刻画

> **复习要点**：复正规算子的结构由复谱定理完整描述（可酉对角化），但实正规算子不能保证对角化——实矩阵可能没有实特征值。因此实正规算子的标准型中允许 $2\times2$ 旋转块。核心结论链：二维实正规非自伴算子的矩阵必为 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$（$b>0$）；正规算子的不变子空间性质（$U$ 不变 $\Rightarrow$ $U^\perp$ 不变，且限制仍正规）；由此通过归纳法得到**实正规算子结构定理**：存在标准正交基使矩阵为分块对角，每个块为 $1\times1$（实特征值）或 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$（$b>0$，对应复共轭特征值对 $a\pm bi$）。作为直接推论，**实正交变换**的标准型：$1\times1$ 块为 $\pm1$，$2\times2$ 块为旋转矩阵 $\begin{pmatrix}\cos\theta&-\sin\theta\\\sin\theta&\cos\theta\end{pmatrix}$（$\theta\in(0,\pi)$）。**复化方法** $V_{\mathbf{C}}=V\otimes_{\mathbf{R}}\mathbf{C}$ 是理解实正规算子的另一途径：实内积复化后成为复内积空间，实正规算子复化后仍正规，可用复谱定理分析，再将共轭特征值对"配对"回 $2\times2$ 实块。

> **知识链**：复谱定理（复正规 $\iff$ 酉对角化）→ 实正规不一定对角化（可能无实特征值）→ 二维实正规非自伴 $\iff$ 矩阵 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$（$b>0$）→ 正规算子不变子空间性质（$U$ 不变 $\Rightarrow$ $U^\perp$ 不变，限制仍正规）→ 实正规算子结构定理（分块对角：$1\times1$ 块 + $2\times2$ 旋转块）→ 实正交变换标准型（$\pm1$ 块 + $\begin{pmatrix}\cos\theta&-\sin\theta\\\sin\theta&\cos\theta\end{pmatrix}$）→ 复化方法（$V_{\mathbf{C}}=V\otimes\mathbf{C}$，复化算子保持正规性）→ 共轭特征对配对回实块

### 10.1 二维实正规算子的刻画

**定理 10.1（二维实正规算子的刻画）** 设 $V$ 是二维实内积空间，$T\in\mathcal{L}(V)$，则以下等价：
1. $T$ 是正规的但不是自伴的；
2. $T$ 关于 $V$ 的**每个**标准正交基的矩阵都有 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$ 的形式，其中 $a,b\in\mathbf{R}$ 且 $b\neq0$；
3. $T$ 关于 $V$ 的**某个**标准正交基的矩阵有 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$ 的形式，其中 $a,b\in\mathbf{R}$ 且 $b>0$。

此时 $T$ 的特征值为 $a\pm bi$（一对共轭复数），且 $T$ 可写为 $T=aI+bJ$，其中 $J$ 满足 $J^2=-I$，$J^*=-J$。

### 10.2 正规算子的不变子空间性质

**定理 10.2（正规算子的不变子空间）** 设 $V$ 是内积空间（实或复），$T\in\mathcal{L}(V)$ 是正规算子，$U$ 是 $V$ 在 $T$ 下不变的子空间，则：
1. $U^\perp$ 在 $T$ 下不变；
2. $U$ 在 $T^*$ 下不变；
3. $(T|_U)^*=T^*|_U$（限制与伴随相容）；
4. $T|_U\in\mathcal{L}(U)$ 和 $T|_{U^\perp}\in\mathcal{L}(U^\perp)$ 都是正规的。

此定理是实正规算子结构定理的归纳基础：一旦找到一维或二维不变子空间，便可正交分解，对其正交补递归应用。

### 10.3 实正规算子的结构定理

**定理 10.3（实正规算子的结构定理——核心）** 设 $V$ 是实内积空间，$T\in\mathcal{L}(V)$，则以下等价：
1. $T$ 是正规的；
2. 存在 $V$ 的一组标准正交基使得 $T$ 关于这组基有**分块对角矩阵**，对角线上的每个块要么是 $1\times1$ 矩阵，要么是形如 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$ 的 $2\times2$ 矩阵，其中 $a,b\in\mathbf{R}$ 且 $b>0$。

**注**：$1\times1$ 块对应 $T$ 的实特征值（此时 $T$ 在该方向上为自伴）；$2\times2$ 块 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$ 对应一对共轭复特征值 $a\pm bi$（$b>0$），该块可理解为"伸缩+旋转"：$a$ 控制缩放，$b$ 控制旋转。

**定理 10.4（实正交变换的结构定理）** 设 $V$ 是实内积空间，$S\in\mathcal{L}(V)$，则以下等价：
1. $S$ 是正交变换（$S^*S=SS^*=I$）；
2. 存在 $V$ 的一组标准正交基使得 $S$ 关于这组基有分块对角矩阵，对角线上的每个块要么是 $1$ 或 $-1$ 构成的 $1\times1$ 矩阵，要么是形如 $\begin{pmatrix}\cos\theta&-\sin\theta\\\sin\theta&\cos\theta\end{pmatrix}$ 的 $2\times2$ 矩阵，其中 $\theta\in(0,\pi)$。

**注**：正交变换是正规且所有特征值模为 $1$ 的特例。在定理 10.3 中取 $a=\cos\theta$，$b=\sin\theta$，且 $a^2+b^2=1$ 即得。$1\times1$ 块 $\pm1$ 分别表示恒等和反射；$2\times2$ 旋转块表示平面上的旋转。

### 10.4 复化方法

**定义 10.1（复化空间）** 设 $V$ 为实线性空间，定义复化 $V_{\mathbf{C}}=V\otimes_{\mathbf{R}}\mathbf{C}=\{u+iv\mid u,v\in V\}$，其上加法和数乘按分量定义。若 $V$ 上有内积 $\langle\cdot,\cdot\rangle$，则复化内积定义为 $\langle u_1+iv_1,u_2+iv_2\rangle_{\mathbf{C}}=\langle u_1,u_2\rangle+\langle v_1,v_2\rangle+i(\langle u_1,v_2\rangle-\langle v_1,u_2\rangle)$。

**定义 10.2（算子的复化）** 设 $T\in\mathcal{L}(V)$，定义 $T_{\mathbf{C}}\in\mathcal{L}(V_{\mathbf{C}})$ 为 $T_{\mathbf{C}}(u+iv)=Tu+i\,Tv$。

**定理 10.5（复化保持正规性）** $T_{\mathbf{C}}$ 的伴随为 $(T_{\mathbf{C}})^*=(T^*)_{\mathbf{C}}$。特别地，$T$ 正规 $\iff T_{\mathbf{C}}$ 正规；$T$ 自伴 $\iff T_{\mathbf{C}}$ 自伴。

**定理 10.6（复化与实标准型的关系）** 对实正规算子 $T$，其复化 $T_{\mathbf{C}}$ 由复谱定理可酉对角化。$T_{\mathbf{C}}$ 的实特征值对应 $T$ 的 $1\times1$ 块；$T_{\mathbf{C}}$ 的成对共轭复特征值 $a\pm bi$（$b>0$）对应 $T$ 的 $2\times2$ 块 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$。由此，实正规算子的结构定理可直接由复谱定理通过"配对"实现在得到。

**定理 10.7（复谱到实形的翻译）** 若 $u+iv$ 是 $T_{\mathbf{C}}$ 对应于 $\lambda=a+bi$ 的特征向量，则 $u-iv$ 是对应于 $\overline{\lambda}=a-bi$ 的特征向量，且 $u,v$ 张成的二维实子空间在 $T$ 下不变，$T$ 在此子空间上关于 $\{u/|u|,v/|v|\}$（适当正交化后）的矩阵为 $\begin{pmatrix}a&-b\\b&a\end{pmatrix}$。

