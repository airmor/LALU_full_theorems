# 注意事项（工作流与格式要求）

- **不默认标注来源**：除非你明确要求，插入到 `LALU_full_theorems.md` 的条目**不添加来源注记**。

- **数学公式（KaTeX）**：所有数学公式使用 KaTeX 语法。
  - 行内公式用 `$...$`，行间公式用 `$$...$$`。
  - 在公式内使用 LaTeX 命令（例如 `\rightarrow`, `\subseteq`, `\operatorname{span}`）时，避免跨行断开或被换行符分隔。

- **LaTeX 命令与反斜杠**：在 Markdown 文本中呈现 LaTeX 命令时，确保命令在 `$...$` 内完整出现；不要在非数学环境中随意转义或重复反斜杠。

- **最小分支映射**：每条从 TeX 提取的定义/定理/引理应映射到其 LaTeX 的“最小分支”（通常为 `\subsection{...}` 或更细的子节）。若目标 MD 中不存在相应最小分支，可新增该子小节以保持与 TeX 层级一致。

- **插入顺序**：在 MD 中插入项时，严格按照源 TeX 文件中出现的顺序（章节与子节内顺序）排列。

- **可新增小节**：允许新增小节以匹配 TeX 的最小分支，但新增小节的标题和位置必须与 TeX 层级一致且风格与现有 MD 保持统一（使用现有的 `#####` / `######` 级别样式）。

- **编号重排**：完成插入后，需要按章节重新编号该章内的 `定义`、`定理`、`引理`（形式如“定义 1.x”、“定理 1.x”），保持编号连续且按出现顺序。

- **避免破坏现有风格**：修改应尽量局限于插入和编号，不要调整无关段落的排版或重写已存在条目的表述。

- **变更前确认**：在对 `LALU_full_theorems.md` 做写操作前，先读取并确认该文件的最新内容以避免覆写（尤其当你并行编辑或曾撤销更改时）。

- **批量修改原则**：对同一章的多个插入操作应在单个 patch 中完成（减少冲突），并在提交后检视一次完整的章节以确认顺序和编号。

- **文件引用风格**：在文档或注记中引用工作区内文件时使用工作区相对路径（例如 `Linear-Algebra-Left-Undone/讲义/...`），必要时再由你决定是否把它们转为外链或添加行号。

- **何时给出来源**：不要给出来源。

- **示例规范（供参考）**：
  - 正确的行内公式示例：$S(n)=n+1$
  - 正确的命题示例：若 $m_0$ 为自然数，若 $P(m_0)$ 成立且对任意 $m>m_0$，由 $P(m')$ 对一切 $m_0 \le m' < m$ 成立可推得 $P(m)$，则 $P(m)$ 对所有 $m\ge m_0$ 成立。

- **其他要求**：每一章都有缺漏，不要少掉任何一章，不要少掉任何 `定义`、`定理`、`引理`。
---

（此文件由自动化助手生成，可按需扩展或迁移到项目文档目录。）